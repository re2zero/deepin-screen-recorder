name: basic_screenshot_tools_suite
app: deepin-screen-recorder
description: 截图工具栏：几何工具（矩形/圆形）、形状工具、颜色选择、撤销
module: basic_screenshot_tools
tags:
- at
- basic
- screenshot
- toolbar
fast_fail: true
env_check: []
setup:
- action: session_start
  command: |
    bash -lc 'python3 "$(git rev-parse --show-toplevel)/tests/at/yaml/helper.py" init'
  wait: 0.5
suites:
- id: basic_screenshot_tools_geometric
  name: 截图工具栏-几何工具按钮显示
  tags:
  - screenshot
  - toolbar
  - geometric
  steps:
  - step_type: action
    action: session_start
    command: |
      bash -lc 'set -e; export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      gdbus call --session --dest com.deepin.ScreenRecorder --object-path /com/deepin/ScreenRecorder --method com.deepin.ScreenRecorder.stopApp >/dev/null 2>&1 || true;
      sleep 0.5'
    wait: 1.5
  - step_type: action
    action: session_start
    command: |
      bash -lc 'export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      xdotool key --clearmodifiers ctrl+alt+a; sleep 1.2;
      xdotool mousemove 320 220 mousedown 1; sleep 0.1; xdotool mousemove 920 620 mouseup 1; sleep 1.0'
    wait: 3.0
  - step_type: action
    action: element_action
    selector:
      accessible_id: gio_button
      role: button
    do: click
  assert_steps:
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: save_local_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: gio_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: rectangle_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: oval_button
      role: button
  - step_type: action
    action: keyboard_press
    key: escape
    wait: 0.3
- id: basic_screenshot_tools_color
  name: 截图工具栏-颜色按钮显示
  tags:
  - screenshot
  - toolbar
  - color
  steps:
  - step_type: action
    action: session_start
    command: |
      bash -lc 'set -e; export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      gdbus call --session --dest com.deepin.ScreenRecorder --object-path /com/deepin/ScreenRecorder --method com.deepin.ScreenRecorder.stopApp >/dev/null 2>&1 || true;
      sleep 0.5'
    wait: 1.5
  - step_type: action
    action: session_start
    command: |
      bash -lc 'export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      xdotool key --clearmodifiers ctrl+alt+a; sleep 1.2;
      xdotool mousemove 320 220 mousedown 1; sleep 0.1; xdotool mousemove 920 620 mouseup 1; sleep 1.0'
    wait: 3.0
  - step_type: action
    action: element_action
    selector:
      accessible_id: gio_button
      role: button
    do: click
  assert_steps:
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: save_local_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: Red_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: Blue_button
      role: button
  - step_type: action
    action: keyboard_press
    key: escape
    wait: 0.3
- id: basic_screenshot_tools_shape
  name: 截图工具栏-形状工具切换
  tags:
  - screenshot
  - toolbar
  - shape
  steps:
  - step_type: action
    action: session_start
    command: |
      bash -lc 'set -e; export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      gdbus call --session --dest com.deepin.ScreenRecorder --object-path /com/deepin/ScreenRecorder --method com.deepin.ScreenRecorder.stopApp >/dev/null 2>&1 || true;
      sleep 0.5'
    wait: 1.5
  - step_type: action
    action: session_start
    command: |
      bash -lc 'export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      xdotool key --clearmodifiers ctrl+alt+a; sleep 1.2;
      xdotool mousemove 320 220 mousedown 1; sleep 0.1; xdotool mousemove 920 620 mouseup 1; sleep 1.0'
    wait: 3.0
  - step_type: action
    action: element_action
    selector:
      accessible_id: gio_button
      role: button
    do: click
  - step_type: action
    action: element_action
    selector:
      accessible_id: rectangle_button
      role: button
    do: click
  - step_type: action
    action: element_action
    selector:
      accessible_id: oval_button
      role: button
    do: click
  assert_steps:
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: save_local_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: oval_button
      role: button
  - step_type: action
    action: keyboard_press
    key: escape
    wait: 0.3
- id: basic_screenshot_tools_undo
  name: 截图工具栏-撤销按钮显示
  tags:
  - screenshot
  - toolbar
  - undo
  steps:
  - step_type: action
    action: session_start
    command: |
      bash -lc 'set -e; export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      gdbus call --session --dest com.deepin.ScreenRecorder --object-path /com/deepin/ScreenRecorder --method com.deepin.ScreenRecorder.stopApp >/dev/null 2>&1 || true;
      sleep 0.5'
    wait: 1.5
  - step_type: action
    action: session_start
    command: |
      bash -lc 'export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      xdotool key --clearmodifiers ctrl+alt+a; sleep 1.2;
      xdotool mousemove 320 220 mousedown 1; sleep 0.1; xdotool mousemove 920 620 mouseup 1; sleep 1.0'
    wait: 3.0
  assert_steps:
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: save_local_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: undo_button
      role: button
  - step_type: action
    action: keyboard_press
    key: escape
    wait: 0.3
- id: basic_screenshot_tools_pin
  name: 截图工具栏-贴图按钮显示
  tags:
  - screenshot
  - toolbar
  - pin
  steps:
  - step_type: action
    action: session_start
    command: |
      bash -lc 'set -e; export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      gdbus call --session --dest com.deepin.ScreenRecorder --object-path /com/deepin/ScreenRecorder --method com.deepin.ScreenRecorder.stopApp >/dev/null 2>&1 || true;
      sleep 0.5'
    wait: 1.5
  - step_type: action
    action: session_start
    command: |
      bash -lc 'export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      xdotool key --clearmodifiers ctrl+alt+a; sleep 1.2;
      xdotool mousemove 320 220 mousedown 1; sleep 0.1; xdotool mousemove 920 620 mouseup 1; sleep 1.0'
    wait: 3.0
  assert_steps:
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: save_local_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: pinscreenshots_button
      role: button
  - step_type: action
    action: keyboard_press
    key: escape
    wait: 0.3
- id: basic_screenshot_tools_scrollshot
  name: 截图工具栏-滚动截图按钮显示
  tags:
  - screenshot
  - toolbar
  - scrollshot
  steps:
  - step_type: action
    action: session_start
    command: |
      bash -lc 'set -e; export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      gdbus call --session --dest com.deepin.ScreenRecorder --object-path /com/deepin/ScreenRecorder --method com.deepin.ScreenRecorder.stopApp >/dev/null 2>&1 || true;
      sleep 0.5'
    wait: 1.5
  - step_type: action
    action: session_start
    command: |
      bash -lc 'export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      xdotool key --clearmodifiers ctrl+alt+a; sleep 1.2;
      xdotool mousemove 320 220 mousedown 1; sleep 0.1; xdotool mousemove 920 620 mouseup 1; sleep 1.0'
    wait: 3.0
  assert_steps:
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: save_local_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: scrollshot_button
      role: button
  - step_type: action
    action: keyboard_press
    key: escape
    wait: 0.3
- id: basic_screenshot_tools_ocr
  name: 截图工具栏-文字识别按钮显示
  tags:
  - screenshot
  - toolbar
  - ocr
  steps:
  - step_type: action
    action: session_start
    command: |
      bash -lc 'set -e; export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      gdbus call --session --dest com.deepin.ScreenRecorder --object-path /com/deepin/ScreenRecorder --method com.deepin.ScreenRecorder.stopApp >/dev/null 2>&1 || true;
      sleep 0.5'
    wait: 1.5
  - step_type: action
    action: session_start
    command: |
      bash -lc 'export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      xdotool key --clearmodifiers ctrl+alt+a; sleep 1.2;
      xdotool mousemove 320 220 mousedown 1; sleep 0.1; xdotool mousemove 920 620 mouseup 1; sleep 1.0'
    wait: 3.0
  assert_steps:
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: save_local_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: orc_button
      role: button
  - step_type: action
    action: keyboard_press
    key: escape
    wait: 0.3
- id: basic_screenshot_tools_ai
  name: 截图工具栏-AI截图按钮显示
  tags:
  - screenshot
  - toolbar
  - ai
  steps:
  - step_type: action
    action: session_start
    command: |
      bash -lc 'set -e; export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      gdbus call --session --dest com.deepin.ScreenRecorder --object-path /com/deepin/ScreenRecorder --method com.deepin.ScreenRecorder.stopApp >/dev/null 2>&1 || true;
      sleep 0.5'
    wait: 1.5
  - step_type: action
    action: session_start
    command: |
      bash -lc 'export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      xdotool key --clearmodifiers ctrl+alt+a; sleep 1.2;
      xdotool mousemove 320 220 mousedown 1; sleep 0.1; xdotool mousemove 920 620 mouseup 1; sleep 1.0'
    wait: 3.0
  assert_steps:
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: save_local_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: ai_assistant_button
      role: button
  - step_type: action
    action: keyboard_press
    key: escape
    wait: 0.3
- id: basic_screenshot_tools_full_toolbar
  name: 截图工具栏-全部工具栏元素显示
  tags:
  - screenshot
  - toolbar
  - full
  steps:
  - step_type: action
    action: session_start
    command: |
      bash -lc 'set -e; export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      gdbus call --session --dest com.deepin.ScreenRecorder --object-path /com/deepin/ScreenRecorder --method com.deepin.ScreenRecorder.stopApp >/dev/null 2>&1 || true;
      sleep 0.5'
    wait: 1.5
  - step_type: action
    action: session_start
    command: |
      bash -lc 'export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      xdotool key --clearmodifiers ctrl+alt+a; sleep 1.2;
      xdotool mousemove 320 220 mousedown 1; sleep 0.1; xdotool mousemove 920 620 mouseup 1; sleep 1.0'
    wait: 3.0
  - step_type: action
    action: element_action
    selector:
      accessible_id: gio_button
      role: button
    do: click
  assert_steps:
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: shot_option_but
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: line_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: pen_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: mosaic_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: text_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: Black_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: Gray_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: White_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: Orange_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: Yellow_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: LightLime_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: Aquamarine_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: Green_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: Purple_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: DarkBlue_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: small_thicknessBtn
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: medium_thicknessBtn
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: big_thicknessBtn
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: scrollshot_tip_help_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: scrollshot_tip_adjust_button
      role: button
  - step_type: action
    action: keyboard_press
    key: escape
    wait: 0.3
- id: basic_screenshot_tools_text_subtoolbar
  name: 截图工具栏-文字工具子工具栏
  tags:
  - screenshot
  - toolbar
  - text
  steps:
  - step_type: action
    action: session_start
    command: |
      bash -lc 'set -e; export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      gdbus call --session --dest com.deepin.ScreenRecorder --object-path /com/deepin/ScreenRecorder --method com.deepin.ScreenRecorder.stopApp >/dev/null 2>&1 || true;
      sleep 0.5'
    wait: 1.5
  - step_type: action
    action: session_start
    command: |
      bash -lc 'export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      xdotool key --clearmodifiers ctrl+alt+a; sleep 1.2;
      xdotool mousemove 320 220 mousedown 1; sleep 0.1; xdotool mousemove 920 620 mouseup 1; sleep 1.0'
    wait: 3.0
  - step_type: action
    action: element_action
    selector:
      accessible_id: gio_button
      role: button
    do: click
  - step_type: action
    action: element_action
    selector:
      accessible_id: text_button
      role: button
    do: click
  assert_steps:
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: AddSizeBtn
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: ReduceSizeBtn
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: FontSizeEdit
      role: text
  - step_type: action
    action: keyboard_press
    key: escape
    wait: 0.3
- id: basic_screenshot_tools_pin
  name: 截图工具栏-贴图模式元素
  tags:
  - screenshot
  - pin
  steps:
  - step_type: action
    action: session_start
    command: |
      bash -lc 'set -e; export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      gdbus call --session --dest com.deepin.ScreenRecorder --object-path /com/deepin/ScreenRecorder --method com.deepin.ScreenRecorder.stopApp >/dev/null 2>&1 || true;
      sleep 0.5'
    wait: 1.5
  - step_type: action
    action: session_start
    command: |
      bash -lc 'export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      xdotool key --clearmodifiers ctrl+alt+a; sleep 1.2;
      xdotool mousemove 320 220 mousedown 1; sleep 0.1; xdotool mousemove 920 620 mouseup 1; sleep 1.0'
    wait: 3.0
  - step_type: action
    action: element_action
    selector:
      accessible_id: pinscreenshots_button
      role: button
    do: click
  assert_steps:
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: pin_save_but
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: pin_close_but
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: pin_ocr_but
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: pin_save_local_but
      role: button
  - step_type: action
    action: keyboard_press
    key: escape
    wait: 0.3
- id: basic_screenshot_tools_ai_panel
  name: 截图工具栏-AI助手面板元素
  tags:
  - screenshot
  - ai
  - panel
  steps:
  - step_type: action
    action: session_start
    command: |
      bash -lc 'set -e; export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      gdbus call --session --dest com.deepin.ScreenRecorder --object-path /com/deepin/ScreenRecorder --method com.deepin.ScreenRecorder.stopApp >/dev/null 2>&1 || true;
      sleep 0.5'
    wait: 1.5
  - step_type: action
    action: session_start
    command: |
      bash -lc 'export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      xdotool key --clearmodifiers ctrl+alt+a; sleep 1.2;
      xdotool mousemove 320 220 mousedown 1; sleep 0.1; xdotool mousemove 920 620 mouseup 1; sleep 1.0'
    wait: 3.0
  - step_type: action
    action: element_action
    selector:
      accessible_id: ai_assistant_button
      role: button
    do: click
  assert_steps:
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: ai_assistant_explain_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: ai_assistant_summarize_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: ai_assistant_translate_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: ai_assistant_askai_button
      role: button
  - step_type: action
    action: keyboard_press
    key: escape
    wait: 0.3

- id: basic_screenshot_tools_toolbar_switch
  name: 截图工具栏-模式切换按钮元素
  tags:
  - screenshot
  - toolbar
  - switch
  steps:
  - step_type: action
    action: session_start
    command: |
      bash -lc 'set -e; export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      gdbus call --session --dest com.deepin.ScreenRecorder --object-path /com/deepin/ScreenRecorder --method com.deepin.ScreenRecorder.stopApp >/dev/null 2>&1 || true;
      sleep 0.5'
    wait: 1.5
  - step_type: action
    action: session_start
    command: |
      bash -lc 'export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
      xdotool key --clearmodifiers ctrl+alt+a; sleep 1.2;
      xdotool mousemove 320 220 mousedown 1; sleep 0.1; xdotool mousemove 920 620 mouseup 1; sleep 1.0'
    wait: 3.0
  assert_steps:
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: recorder_button
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: to_record_but
      role: button
  - step_type: assert
    action: assert_element
    selector:
      accessible_id: to_shot_but
      role: button
  - step_type: action
    action: keyboard_press
    key: escape
    wait: 0.3

teardown:
- action: session_start
  command: |
    bash -lc 'export DISPLAY=${DISPLAY:-:0}; export XAUTHORITY=${XAUTHORITY:-$HOME/.Xauthority};
    python3 "$(git rev-parse --show-toplevel)/tests/at/yaml/helper.py" restore;
    xdotool key --clearmodifiers Escape >/dev/null 2>&1 || true;
    gdbus call --session --dest com.deepin.ScreenRecorder --object-path /com/deepin/ScreenRecorder --method com.deepin.ScreenRecorder.stopApp >/dev/null 2>&1 || true'
  wait: 0.5
- action: session_stop
